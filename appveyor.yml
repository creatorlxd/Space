version: '0.0.2.{build}'

image: Visual Studio 2017

platform:
  - x86
  - x64

configuration:
  - BuildDebug
  - BuildRelease
  - Debug
  - Release
  - UnitTestForDebug
  - UnitTestForRelease

cache:
  - $(APPVEYOR_BUILD_FOLDER)\UnitTestForDebug
  - $(APPVEYOR_BUILD_FOLDER)\UnitTestForRelease
  - $(APPVEYOR_BUILD_FOLDER)\x64\UnitTestForDebug
  - $(APPVEYOR_BUILD_FOLDER)\x64\UnitTestForRelease

build:
  project: Space.vcxproj Source/UnitTest/UnitTest/UnitTest.vcxproj