# To Do List
1. 加入自定义shader组件。需要改动`Window`类

2. 加入配置文件系统，使得可以用配置文件初始化`Window`类。

3. 利用配置文件系统，为`Window`加入帧数控制。

4. 改光照模型为`Forward Plus`。

5. 加入`SubMesh`功能。

6. 加入对`FBX`文件的支持。

7. 游戏场景分割：因为`float`的大小和精度（因为是浮点）限制，而不得不采用的一种采用局部坐标系表示物体坐标的方法。
